% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_fns_misc_BayesMVP.R
\name{fn_nuisance_chunk_layout_positions}
\alias{fn_nuisance_chunk_layout_positions}
\title{Position of every (observation, test) nuisance entry under a given chunking}
\usage{
fn_nuisance_chunk_layout_positions(N, n_tests, n_chunks, vect_type)
}
\arguments{
\item{N}{number of observations}

\item{n_tests}{number of tests (outcomes)}

\item{n_chunks}{number of chunks requested (Model_args_ints\link{4})}

\item{vect_type}{the vectorisation string the C++ reads (Model_args_strings\link{1})}
}
\value{
N x n_tests integer matrix; entry \link{n, t} is the 1-based position of (n, t) in the nuisance vector
}
\description{
The built-in C++ log densities (MVP, MVOP, LC_LT) store the nuisance vector CHUNK BY CHUNK:
chunk k occupies the contiguous segment starting at row_start_k * n_tests, laid out column-major
as a (chunk_size_k x n_tests) block, e.g. MVP_lp_grad_MD_AD_fns_WCP.hpp:
theta_us_vec_ref.segment(row_start * n_tests, chunk_size * n_tests).reshaped(chunk_size, n_tests)
so which (observation, test) an entry belongs to depends on the chunk size. The chunk sizes here
mirror calculate_chunk_sizes() in src/general_functions/structures.hpp exactly.
}
\keyword{internal}
