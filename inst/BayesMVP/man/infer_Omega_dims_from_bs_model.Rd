% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_fns_misc_BayesMVP.R
\name{infer_Omega_dims_from_bs_model}
\alias{infer_Omega_dims_from_bs_model}
\title{Infer the dimensions of the "Omega" parameter(s) of a compiled BridgeStan model}
\usage{
infer_Omega_dims_from_bs_model(bs_model)
}
\arguments{
\item{bs_model}{BridgeStan model object}
}
\value{
list(n_class, n_tests, n_found) - all NULL/0 if no "Omega" parameter exists
}
\description{
The column-reordering pre-burnin needs to know how many latent classes and
how many tests/outcomes the model's correlation matrix has. For the built-in
models this is known from 'model_args_list'; for a USER-SUPPLIED Stan model
nothing is known about the model apart from its parameter names, so the
dimensions are read off the (transformed) parameter names directly:
"Omega\link{c,i,j}" -> n_class = max(c), n_tests = max(i, j);
"Omega\link{i,j}"   -> n_class = 1,      n_tests = max(i, j).
}
